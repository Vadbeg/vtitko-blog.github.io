title: 'Vadim Titko'
tagline: 'Blog'
paginate: 5
permalink: pretty

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-paginate
repository: Vadbeg/vtitko-blog.github.io
domain: vadbeg.github.io     # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://vadbeg.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: ''      # place folder name if the site is served in a subfolder
source: ./source       # source directory
